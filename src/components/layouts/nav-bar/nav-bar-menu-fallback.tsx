/**
 * NavBarMenuFallback - ナビゲーションメニューの読み込み状態を表示します
 *
 * @returns 読み込み中のUI
 */
export default function NavBarMenuFallback() {
  return <div className="h-6 animate-pulse rounded-sm bg-skeleton" />
}
