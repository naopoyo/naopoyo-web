---
name: commit
description: プロジェクトのルールに従ってコミットを作成
---

# コミット作成スキル

このプロジェクトのコミットメッセージルールに従ってコミットを作成します。

## コミットメッセージルール

このプロジェクトでは Conventional Commits 形式を使用しています。

### フォーマット

```text
<type>(<scope>): <説明>
```

### タイプ (type)

- `feat`: 新機能の追加
- `fix`: バグ修正
- `refactor`: リファクタリング（機能追加やバグ修正を含まない）
- `style`: スタイルの変更（CSS、フォーマットなど）
- `chore`: ビルドプロセスや補助ツールの変更
- `docs`: ドキュメントのみの変更
- `test`: テストの追加・修正

### スコープ (scope)

- オプショナル
- 変更対象のコンポーネント名やディレクトリ名を括弧で囲む
- ファイルパスやファイル名ではなく、適切で端的なスコープ名を適用する
- スコープは可能な限り一つとする。一つになるようにコミットを分ける

### 説明

- 日本語で記述
- 簡潔に1行で完結させる
- 何を変更したかを明確に記述

### 詳細な説明（本文）

- 必要に応じて空行を挟んで本文を追加
- 可能な限りリスト形式（箇条書き）で記述する
- 各項目は `-` で始める
- 変更の理由や影響範囲を明確にする

### 例

```text
feat(tools): rem / px 変換ツールを追加
fix(avatar): 画像のパスを修正
refactor(hooks): useClientOnlyフックを追加
style(document-content): h2-h6のリンク前にマージンを追加
chore: update dependencies
```

詳細な説明を含む例:

```text
refactor(auth): 認証ロジックをリファクタリング

- セッション管理を独立したモジュールに分離
- トークン検証のエラーハンドリングを改善
- 不要な依存関係を削除
```

## 実行手順

1. `git status` で変更ファイルを確認
2. `git diff --staged` でステージされた変更を確認（ステージされていない場合は `git diff`）
3. `git log --oneline -5` で最近のコミットスタイルを確認
4. 変更内容を分析し、適切なタイプとスコープを決定
5. コミットメッセージを作成
6. `git add` で必要なファイルをステージ
7. `git commit` でコミットを実行

## 注意事項

- Co-Authored-By は付けないでください。
